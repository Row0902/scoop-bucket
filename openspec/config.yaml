# OpenSpec Configuration — scoop-bucket
# Auto-generated by sdd-init on 2026-06-28

project:
  name: scoop-bucket
  description: >
    Scoop bucket for Windows package manifests (JSON format).
    Uses the ScoopInstaller template with PowerShell automation scripts,
    Pester test framework, and GitHub Actions CI/CD.
  stack:
    - PowerShell (scripts, tests, CI)
    - JSON (Scoop manifests in bucket/)
  conventions:
    - "CRLF line endings (text=auto eol=crlf)"
    - "4-space indentation (2-space for YAML)"
    - "UTF-8 charset"
    - "ScoopInstaller contributing guide"
    - "EditorConfig for consistent formatting"
  ci:
    - "GitHub Actions: CI (push/PR), Excavator (scheduled updates)"
    - "GitHub Actions: PR handler, Issue handler, Issue comment (/verify)"

strict_tdd: true
strict_tdd_reason: "Pester 5.2.0+ test runner detected via bin/test.ps1, active CI pipeline runs tests on push/PR."

testing:
  runner: bin/test.ps1
  framework: Pester (v5.2.0+) with BuildHelpers
  layers:
    unit: true
    integration: false
    e2e: false
  coverage_tool: null
  quality_tools:
    - "markdownlint (via .markdownlint.json)"
    - "PSScriptAnalyzer (VS Code setting)"
    - "EditorConfig (via .editorconfig)"

phases:
  proposal:
    required: true
    artifact: openspec/changes/<change-id>/proposal.md
  spec:
    required: true
    artifact: openspec/changes/<change-id>/spec.md
  design:
    required: true
    artifact: openspec/changes/<change-id>/design.md
  tasks:
    required: true
    artifact: openspec/changes/<change-id>/tasks.md
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true
    artifact: openspec/changes/archive/<change-id>.md
